# CPU-only JAX/TensorFlow for Docker (Linux/macOS dev) — use with Dockerfile target `cpu`.
ttkbootstrap
jax[cpu]==0.5
ott-jax==0.2
# TensorFlow 2.11 + several compiled deps are not compatible with NumPy 2.x.
# Keep NumPy on 1.26 for stable binary compatibility in the macOS dev container.
numpy==1.26.4
ml-collections
six==1.17.0
pyyaml==6.0.2
flax==0.10.3
msgpack==1.1.0
rich==13.9.4
google==3.0.0
protobuf==3.19.6
einops==0.8.1
# 2.11.1 has no Linux aarch64 wheel (pip wants tensorflow-cpu-aws==2.11.1, which was never published).
# 2.11.0 is the last 2.11 line with aarch64; needed for Apple Silicon / arm64 Docker builds.
tensorflow==2.11.0
wrapt==1.17.2
requests==2.32.3
matplotlib==3.10.0
ftfy==6.3.1
regex==2024.11.6
scikit-learn==1.6.1
pandas==2.2.3
seaborn==0.13.2
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.22.0
scipy>=1.11.0
websockify>=0.11.0
tqdm>=4.66.0
chromadb>=0.5.0
dvc>=3.50.0
mlflow>=2.10.0
